# import Movie from "./Movie.graphql"

input IDInput {
    _id: ID!
}

type GetMoviesResponse {
    data: [Movie]!
    error: String
    success: Boolean
}
type Picture {
    thumbnail: String!
    large: String!
}
